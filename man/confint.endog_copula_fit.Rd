% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{confint.endog_copula_fit}
\alias{confint.endog_copula_fit}
\title{Confidence intervals for \code{endog_copula_fit} objects}
\usage{
\method{confint}{endog_copula_fit}(object, parm, level = 0.95, type = c("normal", "percentile"), ...)
}
\arguments{
\item{object}{An object returned by one of the copula estimators.}

\item{parm}{Coefficients to compute intervals for, either names or indices.
Defaults to all coefficients.}

\item{level}{Confidence level. Defaults to \code{0.95}.}

\item{type}{Either \code{"normal"} (normal approximation from bootstrap standard
errors) or \code{"percentile"} (percentile intervals from the bootstrap
replicates).}

\item{...}{Currently unused.}
}
\value{
A matrix with one row per coefficient and columns giving the lower
and upper confidence limits, labelled with the corresponding percentiles.
Coefficients without bootstrap information are filled with \code{NA}.
}
\description{
Computes confidence intervals for the estimated coefficients. The default
\code{type = "normal"} uses a normal approximation based on the bootstrap
standard errors; \code{type = "percentile"} uses percentiles of the bootstrap
replicates stored in \code{object$bootstrap}. Both require the model to have
been fitted with \code{nboots > 0}.
}
\examples{
data(sim_endog)
fit <- CopRegPG(y ~ z_endog | x_exog + w_instr, data = sim_endog,
                cdf = "resc.ecdf", nboots = 25)
confint(fit)
confint(fit, parm = "z_endog", type = "percentile")
}
